<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidfinalproject_20f">


    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".sabiha.EventListActivity"></activity>
        <activity android:name=".sabiha.EventSearchActivity" />

        <activity android:name=".audiosearch.AlbumListActivity"></activity>
        <activity android:name=".audiosearch.ArtistInputActivity" />
        <activity android:name=".audiosearch.EmptyActivity" />
        <activity android:name=".audiosearch.SavedAlbumListActivity" />
        <activity android:name=".audiosearch.SongListActivity" />

        <activity android:name=".chrish.ResultByDate"></activity>
        <activity android:name=".chrish.ViewHistory" />
        <activity android:name=".chrish.CovidCasesData" />
        <activity android:name=".chrish.CovidDataOpener" />
        <activity android:name=".chrish.CovidData" />
        <activity android:name=".chrish.WelcomePageCovid" />

        <activity android:name=".ahmed.RecipeListActivity"></activity>
        <activity android:name=".ahmed.RecipeSearchActivity" />

        <activity android:name=".chrish.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>